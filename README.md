# aachi
Bash scripts to minimally better user experience for common users on Arch Linux with GNOME.

## Installation
On Bash, execute (can be pasted at once)
```bash
git clone https://github.com/elfry2/aachi
cd aachi
chmod +x config.sh install.sh
./install.sh
```
